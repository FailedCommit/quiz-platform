server:
  port: 8091
  servlet:
    context-path: /api/users
#
#jetty:
#  threadPool:
#    maxThreads: 10
#    minThreads: 10
#
spring:
  datasource:
    url: "jdbc:mysql://localhost:3310/user-db?autoReconnect=true"
    username: root
    password: root
#    driverClassName: com.mysql.cj.jdbc.Driver
#    validation-query: select 1;
#    maxActive: 100
  jpa:
    show-sql: false
    generate-ddl: true
    properties:
      hibernate:
        ejb.naming_strategy: org.hibernate.cfg.ImprovedNamingStrategy
        dialect: org.hibernate.dialect.MySQLDialect
        hbm2ddl.auto: validate